name: wireguard_tunnel
description: A fully OS-agnostic Dart package to easily establish and manage local WireGuard VPN tunnels using the native OS CLI, with seamless auto-installation and privilege escalation on Windows, macOS, and Linux.
version: 1.0.0
repository: https://github.com/devsdocs/wireguard_tunnel

environment:
  sdk: ^3.12.0

platforms:
  windows:
  macos:
  linux:

# Add regular dependencies here.
dependencies:
  http: ^1.6.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
